package main

import "fmt"

func main() {
	// arr := []int{1, 2, 3, 4, 5, 7, 8}
	// head := fromArr(arr)
	// l, r := head, head.Next
	// fmt.Println(l, r)
	var a interface{} = 1
	var b interface{} = 1
	fmt.Println(a == b)
}
